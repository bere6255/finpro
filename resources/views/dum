
@section('header')
  @include('inc.nav')
@endsection
@section('content')
  @include('inc.slogan')
@endsection
@section('footer')
  @include('inc.foo')
@endsection
{!!Html::style('node_modules/font-awesome/css/font-awesome.min.css')!!}
{!!Html::style('node_modules/perfect-scrollbar/dist/css/perfect-scrollbar.min.css')!!}
{!!Html::style('css/style.css')!!}
  {!!Html::style('css/custom.css')!!}
  need to install laravelcollective/html for the code above to work
  {!!Html::script('css/custom.js')!!}

for Route
Auth::routes();

Route::get('/home', 'HomeController@index')->name('home');

need to validate password fild for special characters

reg js validation

onsubmit="return reg_validateForm()"



@if (count($errors)>0)
    <?php foreach ($errors ->all() as $regerror): ?>
      <span class="btn table-danger">
          <strong><b>{{ $regerror}}</b></strong>
      </span>
    <?php endforeach; ?>

@endif



<div id="collapse1" class="panel-collapse collapse">
    <div class="modal-body" style="padding:40px 50px;">
      <h3 class="card-title text-primary text-left mb-5 mt-4">Register</h3>
        <form name="reg_form" role="form" method="post" action="/reg4home">
          {{ csrf_field()}}
          <div class="form-group">
            <div class="input-group">
              <span class="input-group-addon"><i class="fa fa-user"></i></span>
              <input type="text" id="reg_usrname" name="reg_usrname" class="form-control p_input" placeholder="Enter you full name" required="true">
            </div>
          </div>
          <div class="form-group">
            <div class="input-group">
              <span class="input-group-addon"><i class="fa fa-envelope-open"></i></span>
              <input type="email" id="email" name="email" class="form-control p_input" placeholder="Enter your email address" required="true">
            </div>
          </div>
          <div class="form-group">
            <div class="input-group">
              <span class="input-group-addon"><i class="fa fa-lock"></i></span>
              <input type="password" id="passwords" name="passwords" class="form-control p_input" placeholder="Enter password" required="true">
            </div>
          </div>
          <div class="form-group">
            <div class="input-group">
              <span class="input-group-addon"><i class="fa fa-lock"></i></span>
              <input type="password" id="passwords_confirm" name="passwords_confirm" class="form-control p_input" placeholder="Repeat Password" required="true">
            </div>
          </div>
            <div class="checkbox">
                <label><input type="checkbox" value="checked" required="true"/>by clicking this check box you agree with the company teams and condition </label>
            </div>
              <button type="submit" class="btn btn-primary btn-block"><span class="glyphicon glyphicon-off"></span> Create Account</button>

        </form>
    </div>
</div>
